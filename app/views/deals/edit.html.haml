= simple_form_for @deal, url: deal_path(@deal), html: {:multipart => true}  do |f|
  %h3.title Update Your Deal
  %fieldset
    = render 'form', f: f
  = submit_tag  "Save", class: 'btn btn-success'
  %a.btn{href: deals_path} Back


