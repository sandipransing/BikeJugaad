= simple_form_for @deal, url: deals_path, html: {:multipart => true}  do |f|
  %h3.title Create Deal
  %fieldset
    = render 'form', f: f
  = submit_tag  "Save", class: 'btn btn-success'
  %a.btn{href: deals_path} Back


